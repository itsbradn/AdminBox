{
  "schemaVersion": 1,
  "id": "adminbox",
  "version": "${version}",
  "name": "AdminBox",
  "description": "Handles all functions on the proxy.",
  "authors": [],
  "contact": {},
  "license": "MIT",
  "icon": "assets/adminbox/icon.png",
  "environment": "server",
  "entrypoints": {
    "main": [
      "gg.braden.adminbox.Adminbox"
    ]
  },
  "mixins": [
    "adminbox.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=${loader_version}",
    "fabric-language-kotlin": ">=${kotlin_loader_version}",
    "fabric": "*",
    "minecraft": "${minecraft_version}"
  }
}
